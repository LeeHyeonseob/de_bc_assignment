전통 카탈로그 4가지 한계 요약 메모

1. 파티션 리스팅
   1. 전통 카탈로그 방식은 파일이 어떤 디렉토리에 존재하는지에 대한 메타데이터를 가지고 있지만 어떤 파일 내부에 찾고자 하는 데이터가 들어있는지는 메타데이터에 저장하고 있지 않다
   2. 따라서 특정 데이터를 찾기 위해 해당 파티션 내를 다 열어서 확인해야한다.
      1. 파일 목록 자체의 조회가 느리다: JPA에서 발생하는 N+1 문제 같이 벌크로 목록을 조회하면 한번만 가져오면 되는데 1번 조회 쿼리시에는 파티션 목록 정보만 조회하고 이 각 파티션에 대해 경로, 포맷, 스키마와 같은 추가적인 메타데이터들에 대한 개별 파티션마다의 조회 쿼리가 발생하여 N번의 쿼리가 발생하여 성능 저하가 발생한다.
      2. 파티션을 찾아도 파티션 내부의 어떤 파일에 찾고자하는 데이터가 있는지 모르기 때문에 해당 파일들을 다 열어봐야한다. 이러한 조회 때문에 성능 저하가 발생한다.
   3. 또한 S3에서는 디렉토리 구조처럼 보이기만 할뿐 key로 되어있기 때문에 더 문제가 심각하다. 디렉토리가 아니라 key에서 prefix가 일치하는 키들을 여러번 스캔해야해서 호출이 추가적으로 발생한다.
2. 파티셔닝 변경 불가
   1. 파티셔닝을 한번 해놓았을 때, 사실상 파티션은 key값이 되어버린다. 따라서 새로운 키값에 할당해서 데이터를 새로 적재한다.
   2. 이는 디렉토리 구조에 논리적 파티션이 포함되는 구조 때문인데, 결국 파티션을 바꿀려면 디렉토리 구조를 다시 작성 -> 데이터가 적게 적재된 경우는 시간적 코스트가 낮지만 장기간 누적된 데이터의 경우에는 이를 재작성하는데 시간적인 코스트가 높고 이는 운영이 집중된 시간에는 서비스 다운을 유발할 수 있다.
3. 동시 쓰기 데이터 유실
   1. data lake 쪽은 트랜잭션의 ACID가 보장되지 않는다. 따라서 한 작업이 완료되기 전에 후속 작업이 이뤄지면 overwrite되는 문제가 발생한다.
4. 롤백 불가
   1. 롤백이 가능하긴 하다. 추가 코스트가 발생하는 버저닝 작업을 통해 버전을 작업하고 수동 작업을 통해 하면 되지만, 이는 2번과 동일하게 누적된 데이터가 많을 때, 시간적인 코스트가 높게 되면서 서비스가 운영되는 동안에 영향을 미칠 가능성이 높아진다.
